namespace Fuse.ExternalEditor.Models.Terrain;

/// <summary>Terrain render mode — mirrors the Python editor's height/veg/water views.</summary>
public enum TerrainMode
{
    Height,
    Veg,
    Water,
}
